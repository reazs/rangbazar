export interface ContactFormInterF {
  name: string;
  email: string;
  request: string;
  message: string;
}

export interface ContactFormErrorItnerF {
  name?: string;
  email?: string;
  request?: string;
  message?: string;
}
